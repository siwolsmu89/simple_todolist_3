<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper>
<mapper namespace="ms.kwon.simple.todolist.mapper.TodoMapper">
    <select id="getTodolist" resultType="ms.kwon.simple.todolist.vo.Todo">
        SELECT id, text, colorValue, checked
        FROM todos
        ORDER BY id DESC
    </select>
    <insert id="addTodo" parameterType="ms.kwon.simple.todolist.vo.Todo">
        INSERT INTO todos
            (id, text, colorValue, checked)
        VALUES
            (#{id}, #{text}, #{colorValue}, #{checked})
    </insert>
    <update id="checkTodo" parameterType="ms.kwon.simple.todolist.vo.Todo">
        UPDATE todos
        SET
            checked = #{checked}
        WHERE
            id = #{id}
    </update>
    <delete id="deleteTodo" parameterType="ms.kwon.simple.todolist.vo.Todo">
        DELETE FROM todos
        WHERE id = #{id}
    </delete>
</mapper>
